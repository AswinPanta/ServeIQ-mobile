# Task 7 Report: Notifications Screen

**Status:** ✅ Complete

**Summary:** Created `app/(tabs)/profile/notifications.tsx` — notification history with sorted list, per-type icons (booking_confirmation→booking, booking_reminder→clock, review_request→star, promotion→tag, system→settings), read/unread dots, "Mark all read" header button, empty state with bell icon, pull-to-refresh via RefreshControl. Uses `useNotifications()` context. Coral (#E63946) theme, 72px items. Zero new TS errors.
